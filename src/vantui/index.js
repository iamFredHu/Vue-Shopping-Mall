import Vue from 'vue'
import {Search, Swipe, SwipeItem, Popup, Icon, Tag, List,Cell, CellGroup,DropdownMenu, DropdownItem} from 'vant'


Vue.use(Swipe).use(SwipeItem).use(Search).use(Popup).use(Icon).use(Tag).use(List).use(Cell).use(CellGroup).use(DropdownMenu).use(DropdownItem)
